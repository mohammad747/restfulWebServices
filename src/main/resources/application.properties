logging.level.org.springframework=info
management.endpoints.web.exposure.include=*
#spring.datasource.url=jdbc:h2:mem:testdb
spring.jpa.defer-datasource-initialization=true
spring.jpa.show-sql=true

spring.datasource.url=jdbc:mysql://localhost:3307/social-media-database
spring.datasource.username=mohammad747
spring.datasource.password=dreamliner787

spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

spring.security.user.name=username
spring.security.user.password=password

#docker run
# -d
# -p 3307:3306
# --name mysql-docker-container
# -e MYSQL_ROOT_PASSWORD=dreamliner787
# -e MYSQL_DATABASE=social-media-database
# -e MYSQL_USER=mohammad747
# -e MYSQL_PASSWORD=dreamliner787
# mysql/mysql-server:latest

# \connect mohammad747@localhost:3307 -> to actually connecting to the database
# \use social-media-database -> to set schema to this database
# \sql -> to switch to sql mode so, we can query the db